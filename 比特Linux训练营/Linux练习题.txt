1.	创建一个shell脚本（称为week4-filesummary.sh），该脚本 带有一个表示文件名的输入参数，执行后，该脚本将在人性化的输出中显示文件的以下属性：filename，file type，file size in type，文件中的行数，文件的最后修改日期（不含时间）。
2.	使用几个存在的文件以及其他情况（例如，文件不存在）从任务1测试脚本。确保脚本具有适当的错误消息，注释和返回值。截取一个屏幕快照，以显示脚本已成功使用，并为丢失的文件返回适当的错误消息。保存屏幕截图 week4-screenshot-filesummary.png。
3.	创建一个将文件名作为输入参数的shell脚本（称为week4-backupusers.sh），执行该脚本时，将读取文件中的所有用户名，并为每个用户创建该用户主目录中所有文件/文件夹的存档，创建一个名为/tmp/bkp/${username}-${date}.tgz的文件。输入文件每行包含一个用户名。创建的存档文件的名称包括日期，格式为'YYYY-MM-DD'，例如/tmp/bkp/scottb-2018-04-23.tgz。
4.	在输入文件中至少有两个不同的用户来测试任务3中的脚本。确保脚本处理适当的错误并且注释正确
